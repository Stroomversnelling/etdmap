# LF everywhere - in the repository and in the working tree.
* text=auto eol=lf

# Windows batch files must keep CRLF to run reliably.
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary - never convert line endings.
*.parquet binary
*.xlsx binary
*.docx binary
*.png binary
*.jpg binary
*.ico binary
